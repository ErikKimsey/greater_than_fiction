import React from 'react';
import { StyleSheet } from 'react-native';
import GyroScope from '../utilities/dynamicBackground';

export const BackgroundStyles = (props) => StyleSheet.create({
  backgroundContainer:{

  }
});