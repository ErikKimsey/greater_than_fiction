import React, { Component } from 'react';
import { View, Text } from 'react-native';
import { getFile } from '../utilities/saveText';

export default class Preview extends Component {
	render() {
		return <View />;
	}
}
